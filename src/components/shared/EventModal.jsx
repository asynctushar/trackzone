import { Dialog } from "@mui/material";
import React from "react";

const EventModal = ({ action, open, handleClose }) => {
	return (
		<Dialog onClose={() => {}} open={true}>
			Hello
		</Dialog>
	);
};

export default EventModal;
